import express from 'express';import cors from 'cors';import fs from 'fs';import path from 'path';import{fileURLToPath}from'url';
const dir=path.dirname(fileURLToPath(import.meta.url)),app=express(),images=path.join(dir,'images'),allowed=new Set(['.jpg','.jpeg','.png','.webp','.gif']);app.use(cors());app.use('/images',express.static(images));
app.get('/api/products',(req,res)=>{const files=fs.readdirSync(images,{withFileTypes:true}).filter(x=>x.isFile()&&allowed.has(path.extname(x.name).toLowerCase())).map(x=>({name:x.name,mtime:fs.statSync(path.join(images,x.name)).mtimeMs})).sort((a,b)=>b.mtime-a.mtime).map(x=>({id:x.name,title:x.name.replace(/\.[^/.]+$/,'').replace(/[-_]+/g,' ').replace(/\b\w/g,c=>c.toUpperCase()),category:'football',url:'/images/'+encodeURIComponent(x.name)}));res.json(files)});app.listen(process.env.PORT||4000,()=>console.log('API on 4000'));
